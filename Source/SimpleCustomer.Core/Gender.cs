namespace SimpleCustomer.Core
{
    /// <summary>
    /// The enum that allows to select a gender.
    /// </summary>
    public enum Gender : byte
    {
        Male,
        Female,
        Unknown,
    }
}
